# Kompyuteringizning lokal tarmoq IP manzili (telefon localhost'ga ulana olmaydi).
# Terminalda `ipconfig getifaddr en0` (Mac) yoki `ipconfig` (Windows) bilan toping.
EXPO_PUBLIC_API_URL=http://192.168.1.50:3000
